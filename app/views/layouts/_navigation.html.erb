<div class="navbar navbar-fixed-top">
  <div class="navbar-inner">
    <div class="container"><!-- Collapsable nav bar -->
      <a class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </a>
 
      <!-- Your site name for the upper left corner of the site -->
      <a class="brand">Comment App</a>

      <!-- Start of the nav bar content -->
      <div class="nav-collapse">
        <ul class="nav">
          <li class="">
            <a href="#posts" class="buy">Posts</a>
          </li>
        </ul>
        <!-- The drop down menu -->
        <% if user_signed_in? %>
        <ul class="nav pull-right">
          <li class="divider-vertical"></li>
          <li class="dropdown">
            <a class="dropdown-toggle" href="#" data-toggle="dropdown">Account<strong class="caret"></strong></a>
            <ul class="dropdown-menu" style="padding: 15px; padding-bottom: 0px;">
              <!-- the form -->
              <li><a href="#posts">Posts</a></li>
              <li><%= link_to 'Edit account', edit_user_registration_path %></li>
              <li><%= link_to 'Logout', destroy_user_session_path, :method=>'delete' %></li>
            </ul>
          </li>
        </ul>
        <% else %>
        <ul class="nav pull-right">
          <li><%= link_to 'Sign up', new_user_registration_path %></li>
          <li class="divider-vertical"></li>
          <li class="dropdown">
            <a class="dropdown-toggle" href="#" data-toggle="dropdown">Sign In <strong class="caret"></strong></a>
            <div class="dropdown-menu" style="padding: 15px; padding-bottom: 0px;">
              <!-- the form -->
              <%= form_for("user", :url => user_session_path) do |f| %>
                <input id="user_email" style="margin-bottom: 15px;" type="text" name="user[email]" size="30" />
                <input id="user_password" style="margin-bottom: 15px;" type="password" name="user[password]" size="30" />
                <input id="user[remember_me]" style="float: left; margin-right: 10px;" type="checkbox" name="user[remember_me]" value="1" />
                <label class="string optional" for="user_remember_me"> Remember me</label>
               
                <input class="btn btn-primary" style="clear: left; width: 100%; height: 32px; font-size: 13px;" type="submit" name="commit" value="Sign In" />
                <a href="/users/password/new">Forgot your password?</a>
              <% end %>
            </div>
          </li>
        </ul>
        <% end %>
      </div>
    </div>
  </div>
</div>


